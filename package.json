{
  "name": "motivational-react-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "seed": "node server/config/seeds.js",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "vite build",
    "render-build": "npm run install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "express": "^4.18.2",
    "framer-motion": "^11.5.2",
    "react-icons": "^5.3.0",
    "semantic-ui-react": "^2.1.5",
    "styled-components": "^6.1.13",
    "vite": "^x.x.x"  // Add this line with the appropriate version
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "react-scripts": "^5.0.1"
  },
  "engines": {
    "node": "14.x"  // Or whichever version you're using
  }
}
